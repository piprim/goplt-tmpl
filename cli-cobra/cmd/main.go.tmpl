package main

import (
	"fmt"
	"os"

	"{{.OrgPrefix}}/{{.Name}}/cmd/cmd"
)

func main() {
	if err := cmd.NewRootCmd().Execute(); err != nil {
		_, _ = fmt.Fprintln(os.Stderr, "error:", err)
		os.Exit(1)
	}
}
